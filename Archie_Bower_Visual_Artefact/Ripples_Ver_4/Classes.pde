// create a class of circle, use for ripple generation
/*
class Circles
{
    //properties
    float diam;
    float xPos, yPos;
    //this is the constructor that builds the objects
    Circles()
    {
        radius = random(20,80);
        xPos = random(width);
        yPos = random(height);
    }
    //the method - methods should be named with verbs
    void display()
    {
        ellipse(xPos, yPos, diam, diam);
    }
}*/
